services:
  claudio:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./config:/config
      - /path/to/games:/games:ro
    environment:
      - ASPNETCORE_URLS=http://+:8080
