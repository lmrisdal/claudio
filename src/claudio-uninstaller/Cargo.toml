[package]
name = "claudio-uninstaller"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "uninstall"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[target.'cfg(target_os = "windows")'.dependencies]
winreg = "0.52"
